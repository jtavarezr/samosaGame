/*
This is just for some  snippet tested here
*/

/*
  return (
    <>
      <h1>Samosa Selector</h1>
      <h2>Count: {count}</h2>

      <div></div>

      <div className="card">
        <img src={samosaImg} className="samosa" onClick={updateCount} />
      </div>
      <div className="container">
        {upgrades.map((upgrade, index) => (
          <div className="upgrade" key={index}>
            <h3>{upgrade.title}</h3>
            <p>{upgrade.description}</p>
            <button onClick={() => setMultiplier(upgrade.multiplier)}>
              {upgrade.cost} samosas
            </button>
          </div>
        ))}
      </div>
    </>
  );*/